#  mamba create -p /nfs/cds-peta/exports/biol_micro_cds_gr_sunagawa/scratch/Projects_NCCR/conda_envs/phynteny -c conda-forge -c bioconda phynteny_transformer=0.1.3
# Phynteny Transformer — refines hypothetical-protein calls using gene synteny.
# Final step of the annotation chain; consumes phold's GenBank output.
channels:
  - conda-forge
  - bioconda
dependencies:
  - phynteny_transformer=0.1.3
